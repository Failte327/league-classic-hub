@model string
<div class="page-head">
    <h1>Resources</h1>
</div>
<nav class="ref-tabs">
    <a asp-page="/Resources/Items" class="@(Model == "Items" ? "active" : "")">Items</a>
    <a asp-page="/Resources/Runes" class="@(Model == "Runes" ? "active" : "")">Runes</a>
    <a asp-page="/Resources/Masteries" class="@(Model == "Masteries" ? "active" : "")">Masteries</a>
    <a asp-page="/Resources/SummonerSpells" class="@(Model == "SummonerSpells" ? "active" : "")">Summoner Spells</a>
</nav>
