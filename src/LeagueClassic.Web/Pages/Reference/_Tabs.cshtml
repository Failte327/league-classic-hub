@model string
<div class="page-head">
    <h1>Reference</h1>
</div>
<nav class="ref-tabs">
    <a asp-page="/Reference/Items" class="@(Model == "Items" ? "active" : "")">Items</a>
    <a asp-page="/Reference/Runes" class="@(Model == "Runes" ? "active" : "")">Runes</a>
    <a asp-page="/Reference/Masteries" class="@(Model == "Masteries" ? "active" : "")">Masteries</a>
    <a asp-page="/Reference/SummonerSpells" class="@(Model == "SummonerSpells" ? "active" : "")">Summoner Spells</a>
</nav>
