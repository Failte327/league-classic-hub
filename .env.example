# Copy to .env for local overrides (this file is safe to commit; .env is git-ignored).
# In production, set the connection string via this env var instead of appsettings:
ConnectionStrings__DefaultConnection=Host=localhost;Port=5433;Database=leagueclassic;Username=leagueclassic;Password=leagueclassic_dev
